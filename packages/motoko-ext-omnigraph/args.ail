module sunholo/motoko_ext_omnigraph/args

import std/json (Json, get, asString)

export func arg_string(args: Json, key: string, fallback: string) -> string {
  match get(args, key) {
    Some(j) => match asString(j) {
      Some(v) => v,
      None => fallback
    },
    None => fallback
  }
}
