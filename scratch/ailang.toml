[package]
name = "local/probe"
version = "0.0.0"
edition = "1"
description = "Scratch probe root for testing sunholo/test_pkg via path dependency"

[exports]
modules = []

[dependencies]
"sunholo/test_pkg" = { path = "../packages/test-pkg" }

[effects]
max = ["IO"]
